# mini-ork Python Framework

> **Moved.** The canonical Python integration doc is now
> [`docs/PYTHON-SDK.md`](PYTHON-SDK.md). It covers both the importable
> **primitives** (verification, dispatch, memory, routing) and the
> **orchestrator** (`MiniOrk` client, `spawn`, `RecipeBuilder`, provider policy,
> the `--json` result contract) in one place.

This page is kept only so existing links resolve — see PYTHON-SDK.md for the
current, maintained reference.
