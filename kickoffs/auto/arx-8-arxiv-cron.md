# ARX-8 Cron-style daemon for arxiv-scout

## Goal

After ARX-0 + ARX-7 ship, `mini-ork run arxiv-scout` should fire weekly via
a system cron OR an internal scheduler tick. Adds `lib/cron-tick.sh` and a
`mini-ork tick` subcommand that the host scheduler triggers.

## Scope Hint

- `lib/cron-tick.sh`

## Verification commands

- `shellcheck lib/cron-tick.sh`

## Done When

- `${MINI_ORK_RUN_DIR}/verdict.json` contains `{ "pass": true }`.
- All verification commands pass in the isolated worktree.

_Auto-generated by `mini-ork epics split` from roadmap-arxiv-self-learn.md._
