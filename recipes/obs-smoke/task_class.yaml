name: obs_smoke
description: >
  Observability smoke test. Smallest possible LLM-touching recipe — exercises
  every emit point so we can assert each obs surface populates correctly.
  Expected wall time: ~30s. Expected cost: ~$0.05-$0.15.

artifact_contract_ref: artifact_contract.yaml
default_workflow: workflow.yaml

matches:
  keywords:
    - obs-smoke
    - observability-smoke
    - smoke
  path_globs: []
  regex: []

default_gates: []
risk_class: low
version: "0.1.0"
