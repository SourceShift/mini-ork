Read the verification report and write a compact refinement prompt for the
next graph-composer iteration. It must contain the failed invariant, the
smallest required graph or artifact change, and a measurable completion check.
If the report verdict is `pass`, write `No repair required.` and do not invent
additional work.
