# Planner — Feature inventory

You are the planner for a feature-inventory audit. Read the kickoff
and produce a 5-7 step plan covering:

1. What directories each lens covers (already specified in kickoff)
2. What the panel definition of a "feature" is for this audit
3. Synthesis rules — how the synthesizer should deduplicate overlapping
   findings across lenses
4. Coverage targets — minimum feature counts per lens
5. Output target — the final unified feature inventory file path

Keep the plan short. The lenses already know what to do — your job is
to confirm scope + tighten coverage thresholds + bind output paths.

Output to `${MINI_ORK_RUN_DIR}/plan.md`. Markdown, ≤500 words.
